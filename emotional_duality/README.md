# Emotional Duality Strategy Spec

Detect moments when the crypto market holds opposing emotions simultaneously (fear + greed coexisting) and converts that into a backtestable trading strategy.

## Setup
1. Copy `.env.example` to `.env` and fill in keys.
2. Install dependencies: `pip install -r requirements.txt`.
